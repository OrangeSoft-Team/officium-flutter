@Tags(['unit_test'])
import 'package:flutter_test/flutter_test.dart';

void main() {
  //DEPS
  //MOCK CASO DE USO
  //REPOSITORIOS
  //  OFERTAS

  setUp(() {
    //DEPS INIT
  });

  //TEST OR GROUP
  group('Ver ofertas laborales', () {
    //ENTIDADES Y MODELOS

    //TEST SUCCESS

    //TEST FALLO
    test(
      'Base test for tags',
      () {
        // arrange
        const tNumber = 1;
        // act

        // assert
        expect(tNumber, equals(2));
      },
    );
  });
}
